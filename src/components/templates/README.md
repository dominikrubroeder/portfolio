# Templates

Templates define the structure and the layout of a page. They _are not_ the page itself yet.

Its purpose is to provide reusability and consume the pages' data.

Examples:

- `home-template` (even though this is not in 'reuse', it follows the principle to ensure consistency when the project
  grows)
