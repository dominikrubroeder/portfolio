# Atoms

The smallest elements of the system. They can't be further broken down into _visual_ UI elements.

They consume design tokens which apply the actual styling and the overall look and feel variables.
