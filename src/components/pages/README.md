# Pages

Pages reference the underlying template and fill them with actual content.

They contain all relevant information about a route / pathname, including metadata.

➡️ See `page.tsx` files in `./src/app` directory
